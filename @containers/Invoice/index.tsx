// Core types
import type { FC } from "react";

// GLobal components
import { ClientTable } from "@components";

const index: FC = ({}) => {
  return <ClientTable />;
};

export { index as NewInvoice };
